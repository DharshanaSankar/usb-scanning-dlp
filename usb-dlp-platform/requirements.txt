# =============================================================================
# Secure USB Monitoring and Data Exfiltration Prevention System - Phase 1
# Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# NOTE: pyudev is Linux-only and will fail to install on Windows/macOS.
#       It is listed with an environment marker so cross-platform
#       installs (e.g. CI matrices) do not break.
# =============================================================================

# --- Core agent dependencies -------------------------------------------------
pyudev==0.24.1; sys_platform == "linux"
watchdog==4.0.1
psutil==6.0.0
python-dotenv==1.0.1

# --- Data + dashboard ---------------------------------------------------------
pandas==2.2.2
plotly==5.22.0
streamlit==1.36.0

# --- Testing ------------------------------------------------------------------
pytest==8.2.2
pytest-cov==5.0.0
